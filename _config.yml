remote_theme: pages-themes/cayman@v0.2.0

plugins:
    - jekyll-feed
    - jekyll-sitemap
    - jekyll-seo-tag
    - jekyll-remote-theme

collections:
    topics:
        output: true

defaults:
    - scope:
        path: ""
      values:
        layout: "default"